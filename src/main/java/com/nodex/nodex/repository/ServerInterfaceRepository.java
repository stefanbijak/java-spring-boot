package com.nodex.nodex.repository;

import com.nodex.nodex.entity.ServerInterface;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ServerInterfaceRepository extends JpaRepository<ServerInterface, Integer> {
}
